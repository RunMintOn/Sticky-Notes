# 任务

## 方向

**工作意图**

让图片成为 Markdown 便签中的可选附件，同时保持以文本为主的编辑体验；粘贴或预览异常不得导致应用退出。

**成功定义**

- 粘贴剪贴板图片后，附件保存到应用数据目录并插入 Markdown 图片语法。
- 正文不实时渲染图片；图片语法前显示可点击的图片图标。
- 点击图标后，以便签中心为锚点显示固定 `420 × 320` 的预览卡片。
- 窄便签不缩小预览卡片；卡片可以越过便签边界，但应受屏幕工作区约束。
- 点击预览卡片外任意位置或按 `Esc` 关闭预览。
- 无效剪贴板、附件或图片格式不会使应用退出。

**约束与偏好**

- [已验证：用户决定] 使用链接前图标，不使用图片轨道。
- [已验证：用户决定] 预览覆盖正文可以接受。
- [已验证：现有架构] Markdown 是规范存储格式，附件位于 `attachments/<note-id>/`。
- 不增加独立图片窗口；查看原图交给系统默认图片查看器。

## 理解

**当前状态与已知事实**

- [已验证：代码检查] 当前实现会在非活动行内联渲染图片。
- [已验证：代码检查] 图片剪贴板读取、PNG 编码和预览解码目前缺少完整的异常边界。
- [已验证：自动复现] 通过真实 `Ctrl+V` 路径粘贴生成的剪贴板位图可稳定触发原始退出。
- [已验证：异常堆栈] `InsertMarkdownImage()` 在插入后读取了已变化的 `SelectionStart`，又增加一次语法长度，将 AvalonEdit 光标设置到文档范围之外；未处理的 `ArgumentOutOfRangeException` 终止了应用。

**系统模型与机制假设**

- [已验证] 保存附件本身成功；退出发生在插入 Markdown 后设置光标的位置计算。
- [推断] 真实外部剪贴板仍可能产生格式读取或编码异常，因此这些输入边界也需要保持非致命。

**重要未知**

- 尚未逐一验证截图工具、浏览器和聊天应用提供的所有剪贴板格式。

## 行动

**候选方向与取舍**

- 内联实时预览：已有实现，但会打断文本阅读，已否决。
- 左侧图片轨道：会持续占用编辑宽度，已否决。
- 链接前图标和居中浮层：关系明确、不长期占用额外区域，已选择。

**当前策略**

保留附件和 Markdown 存储方式，将渲染器改成链接前图标；点击后按需解码并打开固定尺寸浮层，同时收紧剪贴板和图片边界的错误处理。

**下一步行动**

1. 记录图片交互决策。
2. 实现图标、固定预览、外部点击关闭和打开原图。
3. 建立可自动运行的剪贴板粘贴验证；若无法复现原始退出，明确记录验证缺口。

## 验证与更新

**指标与验证方式**

- Release 构建成功。
- 自动化测试通过。
- 隔离数据目录启动应用，粘贴生成的剪贴板图片后进程存活、附件存在且 Markdown 引用已保存。
- 人工检查宽便签和窄便签中的预览尺寸、定位及点击外部关闭。

**执行结果**

- [已验证] 修复图片语法插入后的光标位置计算。
- [已验证] 正文改为显示链接前图片按钮，不再实时解码或内联渲染图片。
- [已验证] 实现以便签为中心、固定 `420 × 320`、允许越过窄便签边界的临时预览。
- [已验证] 预览支持关闭按钮、`Esc`、点击卡片外关闭及调用系统查看器打开原图。
- [已验证：历史执行] 曾通过桌面自动化确认：应用粘贴后存活，附件和 Markdown 已保存；宽度为 280 的便签仍显示 `420 × 320` 预览；点击卡片外关闭。该自动化会占用桌面输入，现已替换为不打开窗口、不发送按键、不移动鼠标且不修改剪贴板的进程内回归测试。
- [已验证] Release 隔离输出构建成功，0 个警告、0 个错误。
- [已验证] Release 测试 16/16 通过。

**需要更新的认识**

原始退出不是预览解码导致，而是插入后光标偏移计算错误。剪贴板和文件异常边界仍作为防御性处理保留。

---

# 后续任务：定向重构

## 方向

在不引入框架或无必要抽象的前提下，减少设置、资源和 Notes List 外壳中的职责混合。重构不得改变现有设置、语言切换、Settings Page、Help Page 或图片功能行为。

## 理解

- [已验证：代码检查] 项目约 3500 行，没有需要按行数机械拆分的超大文件。
- [已验证：代码检查] `UserSettings` 同时负责值与持久化、WPF 外观资源和本地化文本，是最明确的职责混合。
- [已验证：代码检查] `MainWindow.xaml` 同时包含 Notes List、Settings Page 和 Help Page。
- [判断] `MarkdownPresentation` 和 `NoteWindow` 目前较大但内聚，不拆分。
- [判断] `MarkdownEditor` 已委托解析和渲染模块；在出现第二个需要新 seam 的媒体需求前，不继续拆分。

## 行动

1. 先独立提交已有的最小化按钮和演示文档修改。
2. 将本地化文本移入成对的 ResourceDictionary。
3. 将外观资源应用集中到一个内部深模块，由 `App` 连接设置变化。
4. 将 Settings Page 和 Help Page 提取为独立 UserControl。
5. 用不占用桌面输入的构建和进程内测试验证。

## 验证与更新

**执行结果**

- [已完成] 已有修改独立提交为 `a5fb30b`。
- [已完成] 本地化文本迁移到 `Resources/Strings.en.xaml` 和 `Strings.zh-CN.xaml`。
- [已完成] `ApplicationResourceUpdater` 集中处理语言、缩放和文字渲染资源；`UserSettings` 不再依赖 `Application.Current`。
- [已完成] Settings Page 和 Help Page 提取到 `Views/`，各自只接收页面 DataContext 并暴露 `BackRequested`。
- [已完成] 会抢焦点、鼠标或剪贴板的验证被移除；图片插入与语言资源改为 STA 进程内测试。

**最终验证**

- [已验证] Release 隔离构建成功，0 个警告、0 个错误。
- [已验证] 全部自动化测试 17/17 通过。
- [已验证] 英文与中文 ResourceDictionary 键集合一致。
- [已验证] `ApplicationResourceUpdater` 可在进程内由英文切换到中文资源。
- [已验证] 图片插入回归测试通过，验证过程不打开窗口、不发送按键、不移动鼠标且不修改剪贴板。
- [未执行] 重构后的可见 UI 人工验收；为避免打断用户当前桌面操作，本轮没有启动或操控应用窗口。

---

# 后续任务：源文本常驻的图片块

## 目标

图片无需点击即可查看，同时保留并持续显示真实 Markdown 源文本，避免编辑时在“语法”和“图片”之间切换造成布局跳动。

## 决策

- 单独成行的 `![说明](路径)` 在源文本下方显示图片。
- 混在普通文字中的图片语法只显示源文本，不扩展段落。
- 没有 `!` 的普通链接不渲染图片。
- 图片语法增加轻微加深的圆角背景，但仍是真实、可编辑、可选择的文本。
- 删除替换 `!` 的小图标；点击正文图片继续打开固定尺寸大图预览。
- 图片异步解码、限制尺寸、缓存结果；异常仅显示不可用占位。

## 实现

- [已完成] `MarkdownImageSpan` 记录图片是否独占一行。
- [已完成] 图片模块同时负责语法背景、文档空间预留、异步加载、覆盖层定位和大图入口。
- [已完成] 图片插入会在必要时补换行，默认生成可渲染的独立图片行。
- [已完成] 中英文帮助文本和 ADR 已更新。

## 验证

- [已验证] 解析测试覆盖独立图片、行内图片和普通链接。
- [已验证] STA 进程内布局测试确认 Markdown 文本未被替换、图片空间已预留且图片卡位于语法下方。
- [已验证] 离屏渲染确认语法、正文图片及后续文本排列正确；没有打开窗口或操作桌面输入。
- [已验证] Release 隔离构建成功，0 个警告、0 个错误。
- [已验证] 全部自动化测试 18/18 通过。

---

# 后续任务：可调整并记忆尺寸的图片预览

## 目标

让弹出预览明显大于正文图片，并允许用户通过右下角拖拽选择适合自己的尺寸。正文图片不显示卡片背景或边框；深色分组背景只属于 Markdown 图片语法。

## 决策与实现

- [已完成] 默认预览尺寸改为 `720 × 520`，正常最小尺寸为 `420 × 320`。
- [已完成] 右下角增加符合现有灰色设计语言的斜线拖拽提示。
- [已完成] 拖拽结束后将宽高写入 `UserSettings`，其他便签和下次启动复用该尺寸。
- [已完成] 正常退出时立即刷新设置文件，避免 300 ms 防抖期间退出而丢失尺寸。
- [已完成] 正文图片移除可见背景、边框和圆角卡片；不可用占位仍保留提示容器。
- [已完成] 新增 `.gitattributes`，以 `* text=auto eol=lf` 统一仓库文本文件；检查确认现有跟踪文件已全部是 LF，无需制造全仓库换行差异。
- [已完成] 记录 ADR 0007。

## 验证

- [已验证] Release 隔离构建成功，0 个警告、0 个错误。
- [已验证] 全部自动化测试 18/18 通过，包括默认尺寸、尺寸赋值和设置文件持久化。
- [约束] 不启动或操控可见窗口，不操作鼠标、键盘或剪贴板。
- [约束] 本轮不提交，保持修改可供用户检查。

---

# 后续任务：列表纵向缩放与笔记防丢失

## 方向

改善 Notes List 纵向缩放时便签卡反复闪烁、卡顿的观感，并以较低复杂度降低崩溃、写入中断或主数据损坏造成的笔记丢失风险。

## 理解

- [已验证：代码检查] 闪烁只涉及 Notes List；该列表此前启用了回收式虚拟化，视口高度变化会改变已实现的卡片集合。横向缩放和单个 Note Window 没有这一机制。
- [推断] 纵向缩放时回收/重新绑定边缘卡片是当前现象的首要机制解释；视觉闪烁无法由现有无窗口测试直接判定。
- [已验证：代码检查] 正文编辑先经过 350 ms 防抖，再经过存储层 400 ms 防抖；进程突然终止可能丢失最后约 750 ms 的输入。
- [已验证：代码检查] 原保存先写临时文件再替换主文件，正常退出会立即保存，已能避免大部分半写文件问题。
- [已验证：代码检查] 原实现没有历史备份；主 JSON 损坏时只保留 `.invalid`，不会自动恢复可用版本。
- [已验证：只读检查] 当前实际 `notes.json` 是有效 JSON，包含 28 条笔记；没有残留临时文件或损坏文件，但旧版本尚未生成备份。

## 行动

- 对 Notes List 禁用虚拟化，使缩放仅改变裁剪视口，不再因高度变化回收卡片。取舍是极大量笔记时初次列表布局成本增加；相较复杂的自定义虚拟化，这是当前低复杂度方案。
- 保存时显式刷新临时文件到磁盘，再原子替换主文件，同时保留上一版 `notes.json.backup`。
- 主文件缺失或 JSON 损坏时自动读取备份；损坏的主文件继续保留为 `notes.json.invalid`。
- 恢复备份或无法恢复时，在启动阶段显示必须确认的本地化警告，明确说明备份可能缺少最近修改并给出数据目录。
- 图片覆盖层刷新前检查 AvalonEdit 的可视行是否有效，避免恢复验证中发现的 `VisualLinesInvalidException` 导致退出。

## 验证与更新

- [已验证] Release 隔离输出构建成功。
- [已验证] 自动化测试 21/21 通过。
- [已验证] 新测试覆盖上一版本备份、主文件损坏后的保留与恢复，以及没有备份时的明确状态。
- [未验证] 实际拖动 Notes List 上下边缘时的视觉效果；本轮未接管桌面输入，需要人工验收。
- [剩余风险] 突然终止仍可能丢失最后约 750 ms 输入；单份备份不是长期版本历史，也不能替代系统级备份。

---

# 后续任务：LF 规范化与最新版快捷启动

## 方向与理解

- [已验证：代码检查] 仓库文本文件已使用 LF，但笔记正文此前没有统一换行；Windows 输入或粘贴内容可能将 CRLF、CR 和 LF 混合存入 JSON。
- [已验证：机制] 强制结束进程不执行 `OnExit`；最后一次输入经过编辑器 350 ms 和存储层 400 ms 两段防抖，因此立即结束可能丢失约 750 ms 内的修改。
- [选择] 保留防抖以避免每次按键同步刷盘；通过原子替换和备份保护已落盘内容，并明确披露最后一小段输入的剩余风险。
- [选择] 使用桌面启动器，每次启动前从当前源码发布；避免每次改进后手工要求代理运行应用。

## 行动与验证

- [已完成] 编辑器加载文本和 NoteStore 读写边界将 CRLF、孤立 CR 统一为 LF；旧数据在新版本加载并保存后迁移。
- [已完成] 新增 `scripts/launch-latest.ps1`：检测旧实例、发布当前 Release 源码并启动；失败时显示错误，不静默启动旧版本。
- [已完成] 已在 `C:\Users\l3e\Desktop\Win Sticky Notes.lnk` 创建快捷方式，并发布当前版本到 `.artifacts/desktop-app/`。
- [已验证] PowerShell 启动脚本语法有效。
- [已验证] Release 自动化测试 22/22 通过，包括编辑器和存储层 LF 规范化。
- [未执行] 未点击桌面快捷方式，避免在当前旧实例运行时弹窗或启动第二实例。
